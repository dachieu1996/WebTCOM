export interface CreateLanguage {
    code: string;
    name: string;
    imgUrl: string;
}

export interface UpdateLanguage {
    id: number;
    code: string;
    name: string;
    imgUrl: string;
}